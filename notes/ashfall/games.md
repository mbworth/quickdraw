# Ashfall Sector vs the scripted AI

Live hub, 200 map unless noted. Prompt for each game is in `prompts/ashfall/` from game 10 on; earlier prompts lived in chat.

| game | id | model | result | time | notes |
|---|---|---|---|---|---|
| 1-3 | 13, 14, - | opus | loss | | first attempts, one stopped |
| 4 | 16 | opus | win | 9.6 min | economy first, turrets + dug-in riflemen, counter after the biggest wave |
| 5 | | opus | loss | | |
| 6 | | sonnet | loss | | |
| 7 | | sonnet | loss | | |
| 8 | | sonnet | win | | 400 map |
| 9 | | haiku | loss | | |
| 10 | 22 | sonnet | win | 10:00 | first one-call-per-turn prompt (`orders` ending in `wait`); 38 calls, 1 order/call, wait 15, gap 6 s |
| 11 | 23 | sonnet | win | 13.7 min | 30 calls, 2.5 orders/call, waits 10-15, gap 8.7 s; ~25 riflemen lost to guard-reflex chases around a barracks 17 from core; 0 crystal |
| 12 | 28 | sonnet, **quickdraw** | loss | 4.0 min | first pilot game: 77 decisions (19.4/min), reaction p50 4.0 s / p90 7.2 s, model p50 2.0 s, 116 output tokens, packet 660 real (481 est), cache 100% (prefix warm from the smoke run), $0.32; 43 orders sent, 97 dropped client-side (80 `noore`), 4 rejected; the AI's first riflemen (~1:30) killed harvesters one by one at the field while troopers rallied to them one at a time; never a turret, never 8 together |
| 13 | 29 | sonnet, quickdraw | stopped 10.1 min | | game 12 prompt after the review fixes: 214 decisions (21.2/min), reaction p50 3.8 s / p90 6.2 s (wait for the next state push 0.9 s of it), model p50 2.2 s, packet 718 real, 5 timeouts, kept 31.6%, $0.97; 0 reconnects, no pregame calls. Held the anchor with a growing ball and never attacked; stopped by hand |
| 14 | 30 | sonnet, quickdraw | stopped 16.1 min | | rule 3 → attack at 8 riflemen, win by killing the core: 250 decisions (15.5/min), model p50 2.7 s, **39 timeouts**, packet 737 real, kept 26.3%, $1.17. Ball of 27 shuttled between the anchor and a field 40 units out; never saw an enemy building ("map-centre side" is not a coordinate). 27-unit id lists in every move order drove output tokens and timeouts up |
| 15 | 31 | sonnet, quickdraw | **win** | 7.6 min | + enemy base = mirror of my core through 0,0; an unhurt ball keeps going: 147 decisions (19.3/min), reaction p50 4.9 s / p90 7.3 s (wait 1.6 s), model p50 2.6 s, packet 720 real, 7 timeouts, kept 44.3%, 2 rejected, $0.68. Ball of 11 crossed the centre at 6:00, core dead at 7:37 |
| 16 | 32 | sonnet, quickdraw | loss | 10:42 | **M7 ablation 1**: `--full-every 5` (fields + remembered on 1 packet in 5), prompt frozen: 171 decisions (15.9/min), reaction p50 4.7 s / p90 8.9 s (wait 1.4 s), model p50 2.5 s, 132 out tokens, **packet 467 real** (est 304; game 15: 720/583), 23 timeouts (12% vs 4.5%), kept 49.1%, 15 rejected, $0.72. Attacked at 8 riflemen (n=48, 3:00) and again in dribbles of 1-3; the ball traded away around 5-6 min, core died at 10:42. Timeouts sit in the fight (danger triggers, api p90 4.1 s vs 3.2 s), 18 of 23 on slim packets, in line with their 80% share. Rejections: `no units matched` 8, `no harvesters in selection` 5; drops: bad-id 17 (7 in game 15) |
| 17 | 33 | sonnet, quickdraw | **win** | 7:48 | **M7 ablation 2**: `--full-every 5 --ashfall-fold-fields true --ashfall-full-buildings true`, prompt frozen: 158 decisions (20.2/min), **reaction p50 3.9 s** / p90 7.1 s (wait 1.1 s), model p50 2.0 s, 129 out tokens, **packet 427 real** (est 273; slim packets ~250), 14 timeouts (11 of them in the final assault, n=144-158), kept 42.8%, 3 rejected, $0.66. Attacked with 9 at 3:50, reinforced in groups, core dead at 7:48. bad-id drops 15 (same as game 16), rejections back to game-15 level: the model finds its ids in P, A and L without B and F |
| 18 | 35 | sonnet, quickdraw | **win** | 5:48 | ablation 2 config, game 2 of 3: 112 decisions (19.4/min), reaction p50 3.9 s / p90 7.6 s (wait 1.1 s), model p50 2.1 s, 129 out tokens, packet 415 real (est 259), 12 timeouts (9 in the assault, n=91-105), kept 38.2%, 0 rejected, bad-id 9, $0.45. Ball of 5 pushed east at n=59; fastest win yet |
| 19 | 36 | sonnet, quickdraw | **win** | 13:00 | ablation 2 config, game 3 of 3: 251 decisions (19.4/min), reaction p50 4.0 s / p90 7.8 s (wait 1.3 s), model p50 2.1 s, 133 out tokens, packet 508 real (est 353), 23 timeouts, kept 38.7%, **40 rejected** (24 `no ore within 45 units`), $1.04. Home fields dry at ~8:30 with 11 idle harvesters; 22 of the 24 failed gathers were on slim packets, where the model had no node id and gathered blind. Won slowly on a starved economy |
| 20 | 37 | sonnet, quickdraw | loss | 10:24 | ablation 2 config + `--ashfall-fields-on-demand true`: 219 decisions (21/min), reaction p50 4.1 s / p90 7.6 s (wait 1.2 s), model p50 2.0 s, 121 out tokens, packet 467 real (est 334; `F` on 40% of packets), 17 timeouts, kept 36.3%, 14 rejected (`no units matched` 10, **0 blind gathers**: 15 of 19 gathers carried a node id), no-op 12%, $0.88. The flag did its job. The loss is the game-16 shape: pushed east with 3 at 1:30 and 7-8 at 2:40 in separate orders, traded the ball away, core died at 10:24 |
| 21 | 38 | sonnet, quickdraw | loss | 7:54 | game 20 config + step 7 expand fix (labels resolve on the packet's state, `idle` = idle harvesters for gather/repair, tool description names labels): 144 decisions (18.2/min), reaction p50 4.3 s / p90 8.5 s (wait 1.1 s), model p50 2.1 s, 121 out tokens, packet 423 real, 15 timeouts, kept 47.2%, **2 rejected** (14 in game 20), bad-id 12 (all `idle` with no idle harvester, now a client drop), no-op 13%, $0.60. **0 cluster labels used** (8 in game 20, 15-20 in games 15-19); ids per unit-list order p50 8, p90 13: the model lists ids that are not in the packet at all (sequential guesses like #10011, #10015, #10020, mostly alive, so the server takes them). Lost the ball at 4:00 attacking with 15 into the enemy anchor; core died at 7:54 |
| 22 | 39 | sonnet, quickdraw | loss | 6:54 | game 20 config + `--thinking off` (step 2 game 1): 112 decisions (16.3/min), reaction p50 5.6 s / p90 8.7 s (wait 2.0 s), **model p50 2.8 s** (2.1 in game 21), **141 out tokens** (121), packet 397 real, 12 timeouts, kept 38%, 3 rejected, no-op 8%, $0.49. Without adaptive thinking the model writes more (out p90 206 vs 158) and slower per token (19.8 vs 17.5 ms); sent riflemen east in ones and twos from 1:00. Adaptive/low stays |
| 23 | 40 | sonnet, quickdraw | **win** | 4:24 | game 20 config + `--ashfall-no-note true` (step 2 game 2): 82 decisions (18.8/min), reaction p50 5.4 s / p90 8.3 s (wait 1.4 s), model p50 2.7 s, **89 out tokens** (121), packet 396 real, 4 timeouts, kept 41.8%, 0 rejected, no-op 17%, $0.32. Output down a third; model latency did not follow (2.7 s, same as game 22's, so the API was slow this hour). Never built a turret; ball of 11 pushed at 2:55 and killed the core at 4:24, the fastest win |
| 24 | 41 | sonnet, quickdraw | loss | 14:30 | **first full capture** (prompt, schema, raw responses in the run). Bench config: slim + fold + fields on demand + no note + **compact buildings** + expand sugar: 228 decisions (15.7/min), reaction p50 5.6 s / p90 8.7 s (wait 1.5 s), model p50 2.8 s (API slow this hour, as in 22-23), 101 out tokens, packet 467 real, 21 timeouts, kept 42.8%, 1 rejected, no-op 6%, $0.89. **Turret at 2:41, first push at 3:25 with 11 and the turret up**: the first slim game to follow rule 3 as game 15 did (bench prediction held). 25 pushes over 14 minutes, none broke through; core died at 14:30 |
| 25 | 42 | sonnet, quickdraw | stopped 11:12 | - | **order language** (`--ashfall-lang true`, prompt `game25-sonnet.md`, strategy byte-identical to game 15): 277 decisions (24.7/min), reaction p50 **3.7 s** / p90 4.8 s (wait 1.2 s), model p50 2.1 s, **48 out tokens** (101 in game 24), packet 605 real, 2 timeouts, kept 34%, no-op 0%, $0.87. Parse failures 6/277: 4 were the model's leaked tool-call closing tag (now stripped), 2 malformed. Watched live: the ball went to the prompt's example coordinates 65,25 fourteen times after the enemy barracks at 22,49 was seen. Cause in the harness: on this config the remembered layer is on the 1-in-5 cadence and, tying with fields at priority 3, was budget-dropped first, so the M line reached the model on 1 packet of 118 (game 24: 24 of 138; 44 calls had a budget drop against 3). Fixed: `--ashfall-keep-remembered` now puts M on every packet at army priority; new `pushTarget` rule scores it. Also: barracks at 97 s (17 s in game 24) because `t wk 2` before `b ba` starved the build every decision; 483 noore drops as the model orders past its bank. Stopped by hand once the targeting bug was understood |
| 26 | 43 | sonnet, quickdraw | loss | 3:12 | **overlap defect**: `--overlap 2` first live run. 7 decisions in 15 s, then every trigger coalesced for 3 minutes while the core died ($0.03). The decide loop marked a start on each retry and reoffer but a done once; with a count instead of a boolean the engine believed two calls were always in flight. Fixed (one mark per decision, `touch()` for later iterations), regression tests on the engine and the mock game |
| 27 | 44 | sonnet, quickdraw | **win** | 8:00 | **overlap 2** on the game 25 config + keep-remembered + cap 1000: 362 decisions (45/min), reaction p50 **2.5 s** / p90 3.4 s (wait 0.3 s; game 25: 3.7 / 4.8, wait 1.2), model p50 2.0 s, 43 out tokens, packet 649 real, 0 timeouts, 301 of 364 calls overlapped, kept 38%, $1.14 (game 25: $0.87 for 11 min). 81 event calls still waited behind two calls in flight (p50 of those 1.35 s). Pushes: 15 at the mirror -68,-16 and the remembered barracks at -77,-15, 6 at the prompt's example -65,-25. Barracks at 71 s (still the harvester-first opening), turret 3:20, first push 2:51 with 8 and no turret. Win rate confirms; the pace is the result |
| 28 | 45 | sonnet, quickdraw | **win** | 7:00 | **event tick** (`--event-tick`) on the game 27 config: 296 decisions (42/min), reaction p50 **2.3 s** / p90 3.3 s (game 27: 2.5 / 3.4), event wait p50 0.18 s (0.49), 50 of 162 event calls still behind two calls in flight, model p50 2.0 s, 38 out tokens, packet 509 real, 0 timeouts, 229 overlapped, $0.92. Barracks 60 s, turret 2:23, first push 3:08 with 9 and the turret up; 15 pushes at the mirror -59,-38, 9 at the example 65,25. The wait is now the API and the second in-flight slot; the remaining lever is inside the call (output floor ~40 tokens, prefill) |
| 29 | 47 | sonnet, quickdraw | loss (budget-stopped 17:54) | - | **overlap 3** on the game 28 config: 585 decisions (32.8/min), reaction p50 **2.2 s** / p90 3.1 s (game 28: 2.3 / 3.3), model p50 2.0 s, 48 out tokens, packet 780 real, 4 timeouts, 578 overlapped, kept 34%, 32 rejected, $2.00 for 17.9 min ($0.11/min). Event calls behind calls in flight: 51 of 331 (15%; game 28: 50 of 162, 31%), their wait p50 0.94 s. Reaction within one game's noise of overlap 2; the third slot halves the share that waits. Hit `--max-usd 2` at decision 588 and idled (concede-on never) until the stop file; the hub records it as a loss (the AI finished the core after the pilot left). Play: barracks 67 s, first push at 2:38 with 8 and no turret (turret 2:59), 120 pushes, army fed in and dead by 8 min, harvesters gone by 15; ore over 120 65% of the time. Harness defect found: a timed-out call stayed in the pending set after its retry, so the packet's `pending:` line listed up to 6 calls with a cap of 3 (fixed, regression test) |
| 30 | 48 | sonnet, quickdraw | **win** | 6:42 | **streaming dispatch** (`--stream`) on the game 28 config (overlap 2): 292 decisions (43.5/min), reaction p50 2.4 s / p90 3.3 s to the last order, **first order p50 2.2 s** / p90 2.9 s, model p50 1.9 s, 42 out tokens, packet 526 real, 0 timeouts, 236 overlapped, kept 41%, $0.91 ($0.135/min, unchanged). 218 decisions sent orders; 73 sent more than one and on those the first order went out 0.6 s (p50) before the last; single-order decisions gain nothing (the closing quote arrives with the final message). No call died after a partial dispatch. Play: barracks 66 s, turret 3:01, first push 3:01 with 9 and the turret up, 76 pushes, 16 riflemen and 2 turrets at 5:00, core at 6:42. Hub game 47 (game 29) had already ended as a loss on its own: the AI finished the core after the pilot left, so nothing was stale |
| 31 | 49 | sonnet, quickdraw | loss (bust) | 6:42 | **reserved slot** (`--reserve danger,contact,loss`) on the game 30 config: 281 decisions (42/min), reaction p50 2.4 s / p90 3.3 s, first order p50 2.4 s, model p50 1.9 s, 38 out tokens, packet 436 real, 1 timeout, kept 22%, $0.78. The mechanism did its job: danger/contact/loss event calls waited behind calls in flight 30% of the time (game 30: 43%), their wait p50 0.09 s (0.51), p90 1.08 s (1.33). The game itself never trained a rifleman: barracks at 66 s, harassed from 1:27, ore p50 30, workers and depots bought instead of troopers, harvesters dead from 4:00, eliminated at 6:42. Danger-led calls were 48% of all calls at 53% no-op (noore); economy and done triggers fired 121 and 47 times but coalesced 108 and 42, riding into danger-led packets, so `oretrooper` and the idle-barracks trigger rarely led. Whether that starved the opening or the rush would have won anyway is one game's question: not adopted, A/B it inside the win-rate campaign |
| 32 | 50 | sonnet, quickdraw | stopped 15:18 | - | **campaign game 1, `--reserve` on** (working config, game25 prompt): 452 decisions (29.6/min), reaction p50 2.7 s / p90 3.9 s, first order p50 2.4 s, model p50 2.0 s, 48 out tokens, packet 634 real, 3 timeouts, 154 overlapped, kept 35.6%, 61 rejected, $1.47. Stopped by hand: spawn at -14,-69, no enemy building ever seen (`M none` on every packet), and the model pasted the prompt's example coordinate: 132 of 277 moves went to 65,25, where the ball sat idle from 3:06 to the end and grew to 43 riflemen at the supply cap. Barracks 28 s, turret 3:15, first push 2:25 with 8 and no turret. Harness clean (orders accepted 89–94%, cache 100%, hi-class calls behind in-flight 40%). Lesson: the mirror rule's worked example is copied whenever `M` is empty; game32 prompt replaces it with an exploration directive |
| 33 | 51 | sonnet, quickdraw | stopped 6:00 (loss on the hub) | - | **game32 prompt, first wording** ("push to the centre 0,0, then on"), campaign game 1 (not counted), reserve on: 192 decisions (31.8/min), reaction p50 2.8 s / p90 3.9 s, first order p50 2.5 s, model p50 2.0 s, 44 out tokens, packet 510 real, 0 timeouts, 58 overlapped, kept 46%, 15 rejected, cache 100%, hi-class calls behind in-flight 43%, $0.58. The ball reached 0,-1 at 3:06 and was re-sent `am 0,0` 43 times while eight unexplored fields sat on the packet: the literal coordinate in the prompt was the only one the model would write. Lesson: the packet has to name the waypoint (`--ashfall-search-fields`, M line `search fN@x,z`); the prompt carries no coordinate at all |
| 34 | 53 | claude-sonnet-5, quickdraw | loss | 7:18 | campaign 1/20, **--reserve on** (hi-class calls behind in-flight 40%, wait p50 373 ms): 260 decisions (35.8/min), reaction p50 2.6 s / p90 3.5 s (wait 0.4 s), first order p50 2.4 s, model p50 2 s, 38 out tokens, packet 476 real, 1 timeouts, 146 overlapped, kept 27.4%, 8 rejected, cache 100%, $0.7684 |
| 35 | 54 | claude-sonnet-5, quickdraw | **win** | 7:48 | campaign 2/20, **--reserve off** (hi-class calls behind in-flight 51%, wait p50 694 ms): 320 decisions (41.2/min), reaction p50 2.6 s / p90 3.8 s (wait 0 s), first order p50 2.3 s, model p50 2 s, 44 out tokens, packet 530 real, 0 timeouts, 271 overlapped, kept 47.7%, 19 rejected, cache 100%, $1.0332 |
| 36 | 55 | claude-sonnet-5, quickdraw | loss | 5:06 | campaign 3/20, **--reserve on** (hi-class calls behind in-flight 49%, wait p50 526 ms): 175 decisions (34/min), reaction p50 3 s / p90 4.5 s (wait 0.7 s), first order p50 2.8 s, model p50 2 s, 38 out tokens, packet 475 real, 0 timeouts, 81 overlapped, kept 35.1%, 3 rejected, cache 100%, $0.5141 |
| 37 | 56 | claude-sonnet-5, quickdraw | loss | 7:42 | campaign 4/20, **--reserve off** (hi-class calls behind in-flight 38%, wait p50 420 ms): 306 decisions (39.8/min), reaction p50 2.6 s / p90 3.8 s (wait 0 s), first order p50 2.3 s, model p50 2 s, 38 out tokens, packet 491 real, 2 timeouts, 249 overlapped, kept 29.9%, 11 rejected, cache 100%, $0.9068 |
| 38 | 58 | claude-sonnet-5, quickdraw | loss (stopped 7:24) | - | **game38 prompt + `--ashfall-guide`, first live game**: 297 decisions (40.1/min), reaction p50 2.2 s / p90 3 s, first order p50 2.1 s, model p50 1.8 s, 36 out tokens, packet 459 real, 0 timeouts, 218 overlapped, kept 41.4%, 6 rejected, cache 100%, $0.87. Barracks at 12 s (first order of the game), workers 6 by 1:30 and 12 by 4:30, riflemen 9 at 2:30, 19 at 4:30, turret up 4:30; no enemy seen all game until 5:31. Commit at 4:32 with 20 (the gate: 15, turret up, nothing visible) at the mirror -60,-36; met **21 dug-in enemy riflemen** at -40,-36 and lost all 20 in 8 s (4:51–4:59); the AI's first wave (9) arrived at 5:31 on an empty post. Stopped once the shape was clear. Lessons: the AI keeps its whole army dug in at home between waves, so "nothing visible" is the wrong commit signal; the wins committed on the packet where a wave had just died. Prompt revised the same hour: commit only on a `T kill`/`D E-` packet with `X` clear, retreat from a `dug` cluster as big as mine, the turret is the agent's call (no hard line), second barracks before workers past 6 |
| 39 | 59 | script:game38, quickdraw | **win** | 7:06 | **the strategy as code, live** (`--model script:game38`, working config minus stream, $0 API): 421 decisions (59.7/min), reaction p50 0 s / p90 0.1 s (the harness floor: no model latency), kept 75.4%, 20 rejected. Barracks 9 s, 7 workers by 2:00, turret 4:49 (the script's own rule: 6 riflemen and 110 ore after the buy), first commit 4:53 with 19 on a kill packet, ore p50 35, 2 pushes, core dead at 7:06. First evidence that the game38 plan and the packet are sufficient; every further loss on this prompt is the model's share |
| 40 | 60 | script:game38, quickdraw | **win** | 4:30 | campaign 1/10: 259 decisions (58.1/min), reaction p50 0.1 s / p90 0.3 s (wait 0 s), model p50 0 s, 0 out tokens, packet null real, 0 timeouts, 26 overlapped, kept 75.4%, 10 rejected, cache null%, $0 |
| 41 | 61 | script:game38, quickdraw | **win** | 4:48 | campaign 2/10: 245 decisions (50.7/min), reaction p50 0 s / p90 0 s (wait 0 s), model p50 0 s, 0 out tokens, packet null real, 0 timeouts, 16 overlapped, kept 71.4%, 11 rejected, cache null%, $0 |
| 42 | 62 | script:game38, quickdraw | **win** | 6:06 | campaign 3/10: 337 decisions (55.2/min), reaction p50 0 s / p90 0 s (wait 0 s), model p50 0 s, 0 out tokens, packet null real, 0 timeouts, 20 overlapped, kept 68.8%, 16 rejected, cache null%, $0 |
| 43 | 63 | script:game38, quickdraw | **win** | 7:00 | campaign 4/10: 382 decisions (54.2/min), reaction p50 0 s / p90 0.1 s (wait 0 s), model p50 0 s, 0 out tokens, packet null real, 0 timeouts, 23 overlapped, kept 64.6%, 19 rejected, cache null%, $0 |
| 44 | 64 | script:game38, quickdraw | **win** | 5:36 | campaign 5/10: 314 decisions (56.3/min), reaction p50 0 s / p90 0 s (wait 0 s), model p50 0 s, 0 out tokens, packet null real, 0 timeouts, 21 overlapped, kept 72.4%, 17 rejected, cache null%, $0 |
| 45 | 65 | script:game38, quickdraw | **win** | 5:18 | campaign 6/10: 297 decisions (56.3/min), reaction p50 0 s / p90 0.1 s (wait 0 s), model p50 0 s, 0 out tokens, packet null real, 0 timeouts, 31 overlapped, kept 72.7%, 15 rejected, cache null%, $0 |
| 46 | 66 | script:game38, quickdraw | **win** | 5:48 | campaign 7/10: 293 decisions (50.7/min), reaction p50 0 s / p90 0 s (wait 0 s), model p50 0 s, 0 out tokens, packet null real, 0 timeouts, 18 overlapped, kept 69%, 13 rejected, cache null%, $0 |
| 47 | 67 | script:game38, quickdraw | loss | 5:18 | campaign 8/10: 325 decisions (61.2/min), reaction p50 0 s / p90 0 s (wait 0 s), model p50 0 s, 0 out tokens, packet null real, 0 timeouts, 17 overlapped, kept 78.1%, 15 rejected, cache null%, $0 |
| 48 | 68 | script:game38, quickdraw | **win** | 4:42 | campaign 9/10: 227 decisions (48.6/min), reaction p50 0 s / p90 0 s (wait 0 s), model p50 0 s, 0 out tokens, packet null real, 0 timeouts, 13 overlapped, kept 64.6%, 9 rejected, cache null%, $0 |
| 49 | 69 | script:game38, quickdraw | **win** | 5:06 | campaign 10/10: 284 decisions (55.6/min), reaction p50 0.1 s / p90 0.5 s (wait 0 s), model p50 0 s, 0 out tokens, packet null real, 0 timeouts, 21 overlapped, kept 70.5%, 11 rejected, cache null%, $0 |
| 50 | 70 | claude-sonnet-5, quickdraw | **win** | 5:24 | campaign 1/5: 236 decisions (44/min), reaction p50 2.1 s / p90 3 s (wait 0 s), first order p50 2.1 s, model p50 1.7 s, 38 out tokens, packet 481 real, 0 timeouts, 175 overlapped, kept 45%, 1 rejected, cache 100% (8107 written), $0.744 |
| 51 | 71 | claude-sonnet-5, quickdraw | **win** | 5:48 | campaign 2/5: 236 decisions (40.5/min), reaction p50 2.4 s / p90 3.6 s (wait 0 s), first order p50 2.3 s, model p50 1.8 s, 38 out tokens, packet 488 real, 0 timeouts, 175 overlapped, kept 41.3%, 4 rejected, cache 100%, $0.7278 |
| 52 | 72 | claude-sonnet-5, quickdraw | **win** | 11:06 | campaign 3/5: 478 decisions (43.2/min), reaction p50 2.2 s / p90 3 s (wait 0 s), first order p50 2.1 s, model p50 1.7 s, 38 out tokens, packet 630 real, 0 timeouts, 360 overlapped, kept 42.4%, 27 rejected, cache 100%, $1.5892 |
| 53 | 73 | claude-sonnet-5, quickdraw | loss | 13:42 | campaign 4/5: 577 decisions (42.1/min), reaction p50 2.2 s / p90 3.1 s (wait 0 s), first order p50 2.1 s, model p50 1.8 s, 38 out tokens, packet 793 real, 3 timeouts, 447 overlapped, kept 56%, 16 rejected, cache 100%, $1.9969 |
| 54 | 74 | claude-sonnet-5, quickdraw | **win** | 14:36 | campaign 5/5: 529 decisions (36.2/min), reaction p50 2.5 s / p90 4.5 s (wait 0 s), first order p50 2.3 s, model p50 1.8 s, 38 out tokens, packet 841 real, 1 timeouts, 418 overlapped, kept 59.1%, 39 rejected, cache 100%, $1.8548 |
| 55 | 76 | claude-sonnet-5, quickdraw | **win** | 4:54 | **game55 prompt + `--ashfall-plan-marks`** (game38 plus four lines naming `out failed` and `(2nd ba)`), first live game: 204 decisions (41.5/min), reaction p50 2.3 s / p90 3.2 s, first order p50 2.1 s, model p50 1.6 s, 38 out tokens, packet 463 real, 1 timeout, 146 overlapped, kept 34.8%, 4 rejected, cache 100%, $0.61. Barracks 11 s, turret 3:20, first push 16 at 4:06, 10 pushes, ore p50 45, core dead at 4:54: the model's fastest win and its closest to the plan (script agreement, windowed: purchases 79%, army 93%; games 50–54 were 64–84 / 63–88). Neither mark fired: no packet carried `(2nd ba)` or `out failed`, so this game tests the prompt's wording, not the marks |

## Pace
Every Claude Code MCP call costs ~2 s client side (local stdio 1.7 s, live 2.1 s, network 0.2 s). Loop length = model gap + call + wait. Game 11 decided every 27 s.

Quickdraw (game 12, `bin/pace.mjs runs/ashfall-28.jsonl`): 19.4 decisions per game-minute, 8 of 85 calls timed out, reaction p50 4.0 s / p90 7.2 s (event arrival → last result, event-anchored decisions with orders, n=35), model p50 2.0 s, packet 660 real tokens p50 (481 est + framing; above the 600 target). The 2 s between model latency and reaction is queue time: 145 of 326 trigger outcomes waited for a call in flight, and an event waits for the next 500 ms state push. Calls 1 and 2 timed out at the 6 s deadline (cold cache write + strict schema compile); the first call now gets 3× the deadline. Orders kept 27.9%: the prompt's "spend everything" makes the model order purchases the bank cannot pay yet; validate drops them for free, but each is a wasted slot in the batch.

Games 13-15 (same harness, three prompts): the defend-only prompt never attacks; "attack the enemy base" without a coordinate walks in circles; the mirror rule wins. Harness lead from game 14: orders that list 27 ids cost output tokens and deadline timeouts; cluster labels or `army` should be the default selector.

Game 16 (M7 ablation 1, `bin/layers.mjs`): with fields on 1 packet in 5 the packet fell from 720 to 467 real tokens; on slim packets buildings is now the largest repeated layer (66 tokens, 100% repeat), army the largest live one (54). Fields still costs ~300 tokens on the packets that carry it: 5 node ids per explored field.

Game 17 (ablation 2, fold + full buildings + every 5): packet 427 real, est 273, a win at the best reaction time so far (3.9 s p50). Real minus est is a steady ~150 tokens of message framing, now a third of the packet; the next cuts are inside the live layers: army (65 tokens, 23%) and last orders (38, 12%), then node ids and cluster ids.

Ablation 2 over three games (17-19): packet real p50 427 / 415 / 508 (baseline 720), no-op share 12.7 / 8.9 / 7.6%, reaction p50 3.9 / 3.9 / 4.0 s (baseline 4.9), 3 wins, $2.15. The one cost: `gather` needs a node id from `F`, and `F` is absent on 4 packets in 5; game 19 paid for it once its home fields ran dry. Harness answer, `--ashfall-fields-on-demand true`: `F` stays on every packet while a harvester is idle or a worked field is at 0 ore. Offline over games 17-19 it raises `F` presence from 18% to 37 / 28 / 46% of packets and est p50 by 0-9 tokens in games 17-18, by 112 in game 19 (idle harvesters all through its long tail, which the ids would have ended).

Game 20 (fields on demand, live): `F` on 40% of packets, packet 467 real (+40-50 over games 17-18), blind-gather rejections 24 → 0. Loss: the ball left in pieces at 1:30 and 2:40. Across the slim configs (games 17-20) it is 3 wins, 1 loss; game 16 (fields every 5th packet, nothing else) also lost the same way, so the two losses both came from dribbled attacks, a prompt property, not a packet one.

Step 7 (order size), game 21: the expand fix removed the label failures and the `idle` rejections (rejected 14 → 2, label drops 60 → 0 over the recorded games), but the model then used no labels at all and listed ids it had never seen: the packet shows `tr x8@21,33 i` with no ids, the order names 15 ids, 14 of them absent from the packet. Ids are cheap for the model to guess and the server accepts the live ones. Output tokens p50 stayed at 121, so the id lists sit in the p90 (158 vs 121). The harness lever left is the schema: `maxItems` on unit lists forces labels or selectors for big groups.

## Decision quality (2026-09-12, after game 23)
Win rate on the prompt-15 harness: full packet 1/1 (game 15), slim configs 4 wins / 4 losses (games 16-23). `bin/discipline.mjs` scores each run against the frozen prompt's own rules. Game 15 built its turret at 1:34 and pushed at 2:22 with 8 and the turret up. Every slim game (16-21) pushed at 2:10-3:00 with 8-10 riflemen and **no turret**: the turret came at 2:54-5:12 or never (game 23). First pushes were never pieces (always 8-11); the losses came from the ball dying and no second push (games 21-22: 1-2 pushes in the game against 12-51 in the wins).

Rehearsal A/B on the same fixture states (`bin/rehearse.mjs --slim …`, 5 repeats of the four decisive states, $0.45 in total): at t169 the full packet orders the turret 5/5, the slim packet 2/5, slim + anchor lines (core and turret always in `B`, `--ashfall-keep-anchor true`) 5/5. The turret rule needs to see that no turret exists; the prompt reads that from `B`. At t214 the full packet pushes the ball 5/5, slim 2/5, anchor 2/5, anchor + `M` always 3/5: what else the full packet carries (fields, the rest of `B`) is worth 2-3 of 5 pushes there, unexplained at this sample. Pushes go to -65,-25 in every arm: the prompt's example coordinates, not the mirror of the fixture's core at 70,4, so the mirror rule is not computed either way.

### Decision bench (`bin/bench.mjs`, 2026-09-12)
Nine fixed states, each with the order the prompt calls for (`test/games/ashfall/bench/cases.mjs`), five repeats per arm, ~$0.20 an arm. Pass rate is the decision-quality metric; live games are for win rate only.

| arm | pass | est tokens | out tokens | notes |
|---|---|---|---|---|
| full packet | 76% | 448 | 134 | push 0/5 (`attack target:0` when nothing is visible), repair 0/5 (`repair units:["idle"]` with no idle harvester) |
| game 24 config (slim + fold + full buildings + fields on demand + no note + keep anchor) | 58% | 162 | 110 | train 2/5, turret 1/5, dry 1/5: every case that needs to know what buildings exist |
| game 24 config + expand sugar | 69% | 162 | 112 | sugar: `idle` with no idle harvester → the harvester nearest the job; `attack` on a remembered building → attack-move there; attack target described as "visible in X". repair 5/5, push 3/5 |
| slim + fold + fields on demand + no note + **compact buildings** (`--ashfall-compact-buildings true`: one always-on `B` line, ids and types, positions for core and turrets, hp/bld only when hurt or unfinished) | **76%** | 174 | **88** | equal to the full packet at 39% of its tokens and 66% of its output; turret 1/5 (builds the depot first at s17/18, which rule 1 also asks for) |
| compact + **order language** (`--ashfall-lang true`, prompt `game25-sonnet.md`: one string `o`, e.g. `t 12 tr 3; am army 65,25`, decoded client side; strategy sections byte-identical to game 15) | **98%** | 174 | **46** | first run 76% with every miss a parse failure: the model glues arguments on with commas (`am tr x11@46,34,49,50`), puts a label inside a comma list, writes `tr` as a selector, and copied the prompt's example ids (`10006` → `10011` for `ba#11`). Parser accepts the shapes, examples use neutral ids; second run 44/45, latency p50 2.17 s vs 2.79 s |
| two cases from game 25 on the working config (`remembered`: ball of 7 idle at 64,24, barracks remembered at 22,49; `nobarracks`: t60, 150 ore, no barracks) | 80% (8/10) | 256 | 51 | nobarracks 5/5 (on a single state the model builds it; live it starved behind `t wk 2` five decisions running, so the defect is the batch order under `L noore`, not the state reading). remembered 3/5: three attack-moves at 22,49 with the 7-ball, two pull the unhurt ball back to the anchor. The bench was blind to both defects before these cases |

The bench found two harness gaps the games had hidden (the `attack target:0` habit and the `idle` harvester selector) in its first run, and showed that the buildings layer, not fields, is what the prompt's build logic reads. Both fixed in expand and the encoder, no prompt change.

### Trajectory replay (`bin/trajectory.mjs`, game 24 recording, every 3rd decision, 84 decisions an arm)
| arm | agrees with the recording on command kinds | no-op | est tokens | out tokens | cost |
|---|---|---|---|---|---|
| the recorded config replayed on itself | 54% | 23% (recorded 30%) | 306 | 116 | $0.33 |
| full packet (game 15 config) | 36% | 20% | 577 | 146 | $0.42 |
| compact + order language (`--ashfall-lang true`, prompt game25) | 27% | 13% | 306 | **46** | $0.25 |

The noise floor is the finding: with identical prompt, packet and schema the model returns the same kinds of orders on 54% of decisions. Per-decision agreement is therefore not a usable metric; arms have to be compared on rates over many decisions (no-op share, orders per decision, drop reasons, rule adherence), and a difference under ~15 points at this sample is noise. The two arms agree with each other on 36% of decisions; agreement is highest in the opening (minute 0: 6-7 of 8) and lowest in fights (minutes 2-6: 0-2 of 4-6).

Rule adherence over the same rows (`test/games/ashfall/bench/rules.mjs`, pass/applies; the recording scored the same way):

| rule | same config | full packet | order language | recorded |
|---|---|---|---|---|
| turret when none and ore ≥ 110 | 1/2 | 1/2 | 0/2 | 1/2 |
| depot when capped | 1/1 | 1/1 | 1/1 | 1/1 |
| spend when ore ≥ 120 | 10/13 | 8/13 | 11/13 | 7/13 |
| push with 8 idle and turret up | 1/10 | 5/10 | 2/10 | 2/10 |
| whole ball (no pieces) | 1/10 | 6/16 | 4/8 | 2/8 |
| gather with a node id | 3/6 | 5/10 | 13/20 | 1/5 |
| attack only what is visible | 3/9 | 4/9 | 1/2 | 2/2 |
| repair a hurt turret | 0/0 | 0/0 | 0/0 | 0/0 |
| no chase under 8 | 18/23 | 17/23 | 19/23 | 19/23 |

At 84 decisions most rules apply fewer than 15 times, so only `noChase` and `spend` are read at this sample; the push and gather rows need `--every 1` (three times the calls) or several recordings. The same-config arm is the tool's calibration: its rates should match the recording's, and mostly do (the attack row is the exception: 9 attacks against 2 recorded, worth a look).

The order-language arm (same packet, one string out): every rule rate within noise of the same-config arm, no parse drops in 84 decisions, output 46 tokens against 116, model latency p50 2.0 s against 2.9 s. It no-ops less (13% against 23%) and gathers four times as often (20 gathers, 13 with a node id): the bench gate and this one both pass, so game 25 plays it live.

Game 25 recording (94 decisions an arm, every 3rd), scoring the targeting bug: with `--ashfall-keep-remembered true` the pushes go to the remembered barracks at 22,49 (16 of 25) and the new `pushTarget` rule passes 22/25 against 1/14 recorded; 65,25 gets 0. The same arm at `--packet-max 1000`: pushTarget 21/29, latency p50 1.97 s against 1.98 s, every other rule within noise. The cap is not what costs seconds, so it becomes a rail at 1000 and layer cadence and priority decide packet content.

## Prompt strategy review (2026-09-14, after game 37)
Four Opus readers on runs 44–56 (`docs/reviews/2026-09-14-prompt.md`). Losses 31/34/36/37: 8 riflemen left at 2:18–3:09 for a search waypoint that moved (9 hops in game 37, twice onto crystal), no turret up in any of them, died in the open in pieces; the 3:45–4:30 wave found 0–1 defenders; harvesters died in a block; ore banked to 350 against one `queue-full` barracks. Wins 27/28/30/35: 11–31 riflemen committed 12–53 s after a wave died at home, core dead in 17–45 s; every 8-rifleman sally failed; my core took 5 hp all told; the enemy core was within 25 of the mirror every game and the field-first search pointed away 5 of 5 times in game 35. Rewrite: `prompts/ashfall/game38-sonnet.md` (Buy/Army/Keep ladders, commit at 15 after a wave, the rally point as the attack flag, no jargon, no coordinates) with `--ashfall-guide` (counts on H, post/yard on B, mirror-first search). Bench cases changed with the strategy (`push` holds, `remembered` comes home). Bench, amended cases, 5 repeats (2026-09-14; arm files `runs/bench/2026-09-14-*.json`, `--compare` them): game32 68% (opening 0/5, push 0/5, core 0/5, turret 1/5) → game38 + guide 92% (13 cases; `remembered` 1/5 is the one miss, a state the plan never enters: 7 riflemen out with the rally home, the model trains and re-rallies instead of recalling them). Two packet lessons on the way: the model skipped the turret line 4/5 on an absence check until `B` printed `(no tu)`, and answered `-` 7/7 on any `T hb` packet whatever the wording, so under guide a heartbeat is not listed on `T`. Output 40 tokens, est packet 223 (+26 for the guide fields). No live game yet.

## Script vs model (2026-09-14, games 39–54)
The game38 plan as code (`--model script:game38`, `src/games/ashfall/policy/game38.mjs`) against Sonnet on the same prompt, packet and flags, live, back to back on the hub.

| arm | games | wins | minutes | first push (riflemen) | pushes a game | ore p50 | reaction p50 | cost |
|---|---|---|---|---|---|---|---|---|
| script | 11 (39–49) | 10 | 4.5–7.1 | 15–20 | 1–4 | 35–45 | 0 s | $0 |
| Sonnet | 5 (50–54) | 4 | 5.4–14.6 | 7–18 | 14–71 | 45–95 | 2.1–2.5 s | $6.90 |

Model-vs-script agreement on the model's own decisions (`trajectory --model script:game38`): purchases 52–69%, army 58–86%, both 31–45%. The script's loss (game 47) was a 16-rifleman commit into the enemy's home army; the model's (game 53) ran 13.7 minutes to the $2 budget. Reading: the plan and the packet are sufficient (the script wins 10/11 with no memory and no arithmetic); the model's departures are extra pushes, sallies under 15, and skipped purchases, which cost time and money more than games on this sample. The model's games lengthened through the evening while the hub was stuttering (see `ashfall_sector/requests/2026-09-14-live-hub-stutter.md`), so the minutes carry some hub noise.

Departures read with a 6-decision window (a standing order the model gave once counts): purchases agree 64–84%, army 63–88%. Residue: rally out under 5 never recalled (game 52, 120 packets), out clusters not brought home (81), second barracks never built (107), raids outnumbered at home not met (21), and the commit with 8 on a kill packet (game 50, 2:46). Bench cases `sally`/`secondba`/`failed` from those states; Sonnet on game38: 5/5, 0/5, 0/5. `--ashfall-plan-marks` (`out failed` on the rally, `(2nd ba)` on B) + `game55-sonnet.md` naming them: 5/5, 3/5, 4/5; full 16 cases 86% (train and remembered 1/5 as before). Marks without the prompt naming them: no change.

## Opponent profile
Scout ~25 s, riflemen from ~90 s, waves of 6-9 every 15-40 s from ~3 min, from the map-centre side onto the nearest ore field, then parks ~11 from the core. Base is empty right after a wave fails.

## Hub account
Games on the account by hub id: 13–23 (games 1–11, stock agents), 24–25 (checklist, conceded), 27 (smoke), 28–41 (games 12–24; 34 was a stub stopped at 3 decisions), 42 (game 25, stopped by stop file at 11:12, conceded by the next run), 43 (game 26, overlap defect), 44 (27), 45 (28), 47 (29, budget-stopped, the AI finished the core after the pilot left), 48 (30), 49 (31), 50–57 (games 32–37 plus two runs cut at start, 52 and 57, conceded stale), 58 (game 38, stopped at 7:24 once the army was gone, conceded stale by the next run), 59 (game 39, the script, win).
